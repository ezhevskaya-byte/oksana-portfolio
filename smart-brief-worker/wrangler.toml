name = "smart-brief-api"
main = "src/index.js"
compatibility_date = "2026-03-01"

# Secrets (set via wrangler, never commit):
#   wrangler secret put OPENAI_API_KEY
#
# Optional vars (can also set in dashboard):
#   OPENAI_MODEL = "gpt-5.4-mini"

[vars]
OPENAI_MODEL = "gpt-5.4-mini"
